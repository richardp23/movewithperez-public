# Public frontend build variables (safe to expose in browser bundles).
# These are inlined at build time by Astro/Vite.

PUBLIC_FORM_ENDPOINT="https://<worker-name>.<account>.workers.dev"
PUBLIC_TURNSTILE_SITEKEY="0x4AAAA..."
